{
    "name": "@kailash-js/keyboard",
    "version": "1.0.0",
    "description": "",
    "private": true, 
    "main": "index.ts",       
    "scripts": {
      "clean": "rm -r -f dist/lib/*",
      "build":"./../../../node_modules/typescript/bin/tsc --project tsconfig.production.json",
      "clean-build":"yarn clean && yarn build",
      "publish-package": "yarn clean && yarn build && cd dist && npm publish"
    },
    "dependencies": {
      
    },
    "peerDependencies": {
      "@kailash-js/react-native-android-keyboard-adjust": "^1.0.2"
    }
  }
  