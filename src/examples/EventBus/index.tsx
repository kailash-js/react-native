import React from 'react';
import {Text, View} from 'react-native';

export const EventBusScreen = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>Event Bus Screen</Text>
    </View>
  );
};
